/WebContent/WEB-INF/views ->it will contain all the jsp files and html files
/src/resources -> it will contain all the resources required for the project
Constant.java file is added.
SlamBookUserNameException.java is added.
/WebContent/WEB-INF/lib -> it will contain all the .jar files required.